
// from src/-/wrapper.js

/*
 * library wrapper
 */
var wrapper = {};

wrapper.Graph = Graph;

wrapper.Graph.defaultOptions = {
  size : 1000,
  thresold : 10,
  factor : 4
};

// return wrapper;
ctx.Retiare = wrapper;
